{
  "scripts": {
    "start": "npm run build && node .",
    "dev": "npm run build && nodemon",
    "build": "cd react-build && npm run build"
  },
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
